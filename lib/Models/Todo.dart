class Todo {
  final String title;
  final String description;

  Todo(this.title, this.description);
}

class Tryout {
  final String Title;
  final String description;
  Tryout(this.Title, this.description);
  
}